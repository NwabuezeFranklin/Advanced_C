#include<stdio.h>
#include<stdlib.h>

//No days off
//Retribution

int main()
{
    char d[30], f[30];
    printf("Enter your first name:");
    scanf("%s %s", &d, &f);
    printf("Your  name is %s %s\n", d, f);
}